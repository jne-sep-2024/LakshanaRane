var user = {
    name: "lakshana",
    email: "laksh@mail.com",
    isActive: true
};
function createUser(user) {
    console.log("".concat(user.name, " is my name with email: ").concat(user.email, " with isactive status:").concat(user.isActive));
}
// createUser({name:"laksh",isPaid:true}); 
// createUser():{}{}  syntax
